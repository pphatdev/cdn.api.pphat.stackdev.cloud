import { labels, modules } from "../utils/modules.js";

export class Controller {
    static defaultConfig = {
        title: 'StackDev',
        page: 'dashboard',
        modules,
        labels,
    }
}