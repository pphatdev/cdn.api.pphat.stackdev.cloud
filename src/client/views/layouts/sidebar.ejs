<aside class="w-64 bg-sidebar border-r border-slate-200 flex flex-col p-6 overflow-y-auto">
    <div class="flex items-center gap-2 mb-10 text-blue-600">
        <div class="bg-blue-600 p-1.5 rounded-lg">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path></svg>
        </div>
        <span class="text-xl font-bold text-slate-900">CloudBox</span>
    </div>

    <nav class="space-y-1 mb-10">
        <% modules.forEach(function(module) { %>
            <a href="<%= module.url %>" class="<%= currentPath === module.url ? module.activeClassName : module.className %>">
                <%- module.icon %>
                <%= module.name %>
            </a>
        <% }); %>
    </nav>

    <div class="mb-10">
        <h3 class="text-xs font-semibold text-slate-400 uppercase tracking-wider mb-4 px-3">Labels</h3>
        <div class="space-y-1">
            <% labels.forEach(function(label) { %>
                <a href="<%= label.url %>" class="flex items-center gap-3 px-3 py-2 text-slate-500 hover:bg-slate-100 rounded-lg transition-colors">
                    <span class="w-2 h-2 rounded-full <%= label.color %>"></span>
                    <%= label.name %>
                </a>
            <% }); %>
        </div>
    </div>

    <!-- <div class="mt-auto bg-white p-4 rounded-xl border border-slate-200">
        <div class="flex items-center justify-between mb-2">
            <div class="flex items-center gap-2">
                <svg class="w-4 h-4 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z"></path></svg>
                <span class="text-sm font-semibold">Storage</span>
            </div>
            <span class="text-xs font-bold text-slate-500">75%</span>
        </div>
        <div class="w-full bg-slate-100 h-1.5 rounded-full mb-2 overflow-hidden">
            <div class="bg-blue-600 h-full w-[75%] rounded-full"></div>
        </div>
        <p class="text-[10px] text-slate-400 mb-4">150 GB of 200 GB used</p>
        <button class="w-full py-2 bg-slate-50 border border-slate-200 rounded-lg text-sm font-semibold hover:bg-slate-100 transition-colors">Upgrade Plan</button>
    </div> -->
</aside>